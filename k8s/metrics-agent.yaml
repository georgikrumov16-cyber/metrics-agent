
apiVersion: v1
kind: Service
metadata:
  name: metrics-agent
  labels:
    app: metrics-agent
spec:
  selector:
    app: metrics-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
